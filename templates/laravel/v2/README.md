# Laravel Template (v2)

This template is a bootstrap recipe that uses Composer to scaffold a full Laravel app at install time:
- `composer create-project laravel/laravel .`

This keeps the repo lightweight while still producing a conventional Laravel scaffold.

