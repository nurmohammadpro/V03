import React from "react";
import ReactDOM from "react-dom/client";

function App() {
  return <main style={{ padding: 24 }}>V03 MERN Template</main>;
}

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
);

